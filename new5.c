// Write a program to print the ASCII value of a given character entered by the user
#include<stdio.h>
int main()
{
      char ch;
      printf("Enter any character ");
      scanf("%c",&ch);
      printf("The character %f ASCII value is %d", ch,ch);
      return 0;
}